[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "aicrusaders"
version = "0.2.0"
description = "Scaffold for designing, running and evaluating Human-Machine Collaboration frameworks driven by dynamic power handover."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
authors = [{ name = "NoahIsARider" }]
keywords = ["human-machine-collaboration", "handover", "frameworks", "evaluation", "seci", "orchestration"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = []

[project.optional-dependencies]
llm = ["openai>=1.0"]
gui = ["fastapi>=0.110", "uvicorn>=0.29"]
dev = ["pytest>=7.0", "pytest-cov>=4.0"]

[project.urls]
Homepage = "https://github.com/NoahIsARider/Crusaders"
Documentation = "https://github.com/NoahIsARider/Crusaders/blob/master/docs/index.md"
Repository = "https://github.com/NoahIsARider/Crusaders"

[project.scripts]
crusaders-demo = "crusaders.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
